#include <unistd.h>
#include <sys/wait.h>
#include <time.h>
#include <stdio.h>

int main (void)
{
    /*int pid = fork();
    int n;
    int i;
    i = 0;
    if (pid == -1)
        return 1;
    if (pid == 0)
        n = 1;
    else
        n = 6;
    //wait(NULL);    

    while (i < 5)
    {
        printf("%d", n);
        fflush(stdout);
        n++;
        i++;
    }
    if (pid != 0)
        printf("\n");*/

    //printf("%c\n", 75);
    return (0);           
}